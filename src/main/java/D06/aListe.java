package D06;

public class aListe {
     /*
        replace - replaceAll : içindeki datayı değiştirmeye yarar
        trim: baştaki ve sonraki boşlukları silmeye yarar
        toLowerCase : bütün harfleri küçük harf yapar
        toUpperCase: bütün harfleri büyük harf yapar
        split: içine girilen karakterden bölüp sağ veya sol şeklinde seçim yaptırır(0 sol) (1 sağ)
        contains: içerme durumunu kontrol eder. bu o string de var mı yok mu
        equals(==):eşit mi diye kontrol eder (==:  hem adrese hem degere bakar
        equalsIgnoreCase: büyük ahrf küçük harf fark etmeden girilen kelşmeler ayn mı eşit mi kontrol eder
        subString = ilk girilen index ten ikinci ideze kadar olan değeri döndürür.eğer2.idexi girmessek sonuna kadar alır
        startsWith= ne ile başaldığını kontrol eder
        endsWith= ne ile bittiğini kontrol eder
        lenght = uzunluğu kontrol eder. toplam karakter sayısı
        lenght()-1 = son kararkteri verir
        indexOf = girilen karakterin il gördüğü indexini verir
        lastIndexOf = girilen karakterin son gördüğü indexi verir
        concat : birleştirme işlemi yapar
        isBlank = hem boş mu diye, eğer boş değilse ; sadece boşluk karakteri var mı diye kontrol eder
        isEmpty = karakter var mı, boş mu diye kontrol eder(hiçlik)
        charAt() = girilen indexteki karakteri almak için
        valueOf=String'i başka veriable'lara cevirmeye yarar

      */
}
